# Core API
fastapi==0.110.0
uvicorn[standard]==0.29.0
gunicorn==21.2.0
requests==2.31.0

# Optional: parsing & compression support
python-multipart==0.0.9   # if you ever accept file uploads
pyyaml==6.0.1             # for YAML manifest handling

# DeepStrip internals
# (if you have your own deepstrip_4430_beta19.py in repo, you don’t need a pip package)
